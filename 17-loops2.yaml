- name: lopps in yaml
  hosts: frontend
  become: yes
  tasks:
   - name: loops
     ansible.builtin.debug:
      msg: "Hello {{ item }}"
     loop:
      - adam
      - shafi
      - rahamtulla
      - ayesha
      












# - name: lopps intall package

#   hosts: frontend
#   become: yes
#   tasks:
#    - name: install packages
#      ansible.builtin.package:
#       name: "{{ item }}"
#       state: installed
#      loop:
#       - mysql
#       - nginx
#       - zip
